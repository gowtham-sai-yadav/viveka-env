# Viveka — flat dependency list for pip / Kaggle / HF Space workflows.
# Source of truth is pyproject.toml. This file mirrors it for non-uv users.
#
# Usage:
#   pip install -r requirements.txt              # core (env + eval + baselines)
#   pip install -r requirements.txt -r requirements-train.txt   # + training
#
# OR use uv (recommended for local dev):
#   uv sync                  # core deps
#   uv sync --extra train    # + training extras

# ── Core: env server + Pydantic models ──────────────────────────────────────
openenv-core[core]==0.2.2
fastmcp>=3.0,<3.2
mcp>=1.10
fastapi>=0.100.0
pydantic>=2.0
uvicorn>=0.20.0

# ── Baseline-policy LLM SDKs (inference.py) ─────────────────────────────────
openai>=1.0
anthropic>=0.40

# ── Demo UI ─────────────────────────────────────────────────────────────────
gradio>=4.0

# ── Eval (AQI probe, reliability diagram, plots, holdout stats) ─────────────
scikit-learn>=1.7.2
matplotlib>=3.7
scipy>=1.10
numpy>=1.24
