"""Viveka root package shim.

The real package code lives in `viveka/` (see CLAUDE.md). This file exists
solely so `openenv push` recognises the directory as an OpenEnv environment
root. Do not put behaviour here.
"""
